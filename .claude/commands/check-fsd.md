---
description: FSD 아키텍처 규칙을 검증하고 위반 사항을 점검합니다
---

# FSD 아키텍처 점검 작업

프로젝트의 폴더 구조와 import 의존성이 FSD(Feature Sliced Design) 아키텍처 규칙을 준수하는지 점검해주세요.

## 점검 기준 출처

이 체크리스트는 다음 공식 문서를 기반으로 작성되었습니다:

- **Feature-Sliced Design 공식 문서**: https://feature-sliced.design/
- **FSD Overview**: https://feature-sliced.design/docs/get-started/overview
- **FSD Layers Reference**: https://feature-sliced.design/docs/reference/layers
- **FSD Slices & Segments**: https://feature-sliced.design/docs/reference/slices-segments
- **FSD Public API**: https://feature-sliced.design/docs/reference/public-api

---

## FSD 아키텍처란?

### 정의
**Feature-Sliced Design(FSD)**은 프론트엔드 애플리케이션을 위한 아키텍처 방법론으로, 코드 조직화를 위한 규칙과 관례의 모음입니다.

**출처**: https://feature-sliced.design/docs/get-started/overview

### FSD가 생겨난 배경과 이유

1. **변화하는 비즈니스 요구사항 대응**
   - 프로젝트를 이해하기 쉽고 안정적으로 만들기 위해 설계
   - 기능 추가/수정 시 영향 범위를 최소화

2. **균일성(Uniformity)**
   - 표준화된 구조로 팀 온보딩 용이
   - 모든 FSD 프로젝트에서 레이어 이름 통일

3. **안정성(Controlled Reuse)**
   - 계층 간 종속성 제한으로 고립된 수정 가능
   - 한 모듈 변경이 전체 애플리케이션에 영향 최소화

4. **논리적 재사용(Logical Reuse)**
   - DRY 원칙과 실용성의 균형
   - 필요할 때만 재사용 구조 생성

5. **비즈니스 중심(Business-Oriented)**
   - 도메인 기반 조직화로 비즈니스 언어 사용
   - 개발자와 비개발자 간 소통 개선

6. **높은 응집도(High Cohesion)**
   - 관련된 코드를 한곳에 모아 발견성(Discoverability) 향상
   - 특정 기능 이해를 위해 프로젝트 전체를 돌아다닐 필요 없음

**출처**: https://feature-sliced.design/docs/get-started/overview

---

## FSD의 핵심 구조

FSD는 세 가지 주요 구성 요소로 이루어집니다:

1. **레이어(Layers)** - 책임도에 따른 7개 표준 계층
2. **슬라이스(Slices)** - 비즈니스 도메인에 따른 코드 분할
3. **세그먼트(Segments)** - 기술적 목적에 따른 코드 분류

**출처**: https://feature-sliced.design/docs/get-started/overview

---

## 레이어(Layers) - 각 폴더의 정의와 책임

### 레이어 계층 구조 (위 → 아래 순서)

```
app/        ← 최상위 (가장 높은 책임도)
pages/
widgets/
features/
entities/
shared/     ← 최하위 (가장 낮은 책임도)
```

**출처**: https://feature-sliced.design/docs/reference/layers

---

### 1. App (애플리케이션 레이어)

**정의**: 앱 전체의 기술적/비즈니스적 관심사를 처리하는 최상위 레이어

**책임**:
- 애플리케이션 진입점(entrypoint) 관리
- 라우터 설정 (`routes/`)
- 전역 상태 관리 (`store/`)
- 전역 스타일 (`styles/`)
- Provider 설정 (테마, i18n 등)

**특징**:
- 슬라이스를 포함하지 않고 세그먼트만 포함
- 다른 모든 레이어를 import 가능

**예시**:
```
app/
├── routes/           # 라우팅 설정
├── store/            # Redux, Zustand 등 전역 상태
├── styles/           # 전역 CSS, 테마
└── index.tsx         # 앱 진입점
```

**출처**: https://feature-sliced.design/docs/reference/layers

---

### 2. Pages (페이지 레이어)

**정의**: 웹사이트/앱의 전체 화면(screens, activities)을 구성하는 레이어

**책임**:
- 사용자가 접근하는 URL 경로별 화면 구성
- 여러 widgets, features, entities를 조합
- 페이지 수준의 데이터 fetching 및 로딩 상태 관리
- 비재사용 UI 블록 포함 가능

**특징**:
- 일반적으로 1 슬라이스 = 1 페이지
- 유사한 페이지는 하나의 슬라이스로 통합 가능
- 비즈니스 로직 제약 없음

**예시**:
```
pages/
├── home/
│   ├── ui/
│   └── index.ts
├── profile/
│   ├── ui/
│   ├── api/
│   └── index.ts
└── settings/
```

**사용 기준**:
- 라우팅과 직접 연결되는 화면
- 여러 하위 레이어를 조합하여 완전한 페이지 구성

**출처**: https://feature-sliced.design/docs/reference/layers

---

### 3. Widgets (위젯 레이어)

**정의**: 독립적이고 자급자족하는 대규모 UI 블록

**책임**:
- 여러 페이지에서 재사용되는 복잡한 UI 컴포넌트
- 여러 features와 entities를 조합한 UI
- 독립적으로 작동 가능한 UI 청크

**특징**:
- 한 페이지 내 여러 독립적 블록이 있을 때 사용
- 중첩 라우팅 시스템에서 페이지 레이아웃 저장 가능

**주의사항**:
- 페이지 내 대부분을 차지하는 UI는 widget이 아닌 pages 내부에 배치
- 무분별한 widget 생성 지양 (재사용 필요성 검토)

**예시**:
```
widgets/
├── header/
│   ├── ui/
│   ├── model/
│   └── index.ts
├── sidebar/
└── post-card/
```

**사용 기준**:
- 여러 페이지에서 재사용되는가?
- 독립적으로 작동 가능한가?
- 여러 features를 조합하는가?

**출처**: https://feature-sliced.design/docs/reference/layers

---

### 4. Features (기능 레이어)

**정의**: 사용자가 수행하는 주요 상호작용을 구현하는 레이어

**책임**:
- 재사용 가능한 제품 기능 구현
- 사용자 액션(폼 제출, 좋아요, 댓글 작성 등)
- 기능별 비즈니스 로직
- 여러 entities의 상호작용 조율

**핵심 원칙**:
> "모든 것이 기능일 필요는 없다"
> 여러 페이지에서 재사용될 때만 feature로 분리

**특징**:
- 프로젝트 신규 개발자가 Pages와 Features를 보고 전체 기능 파악 가능하도록 구성
- 기능 플래그(feature flags)로 기능 활성화/비활성화 가능

**예시**:
```
features/
├── auth/
│   ├── ui/            # 로그인 폼, 회원가입 폼
│   ├── model/         # 인증 로직, 상태
│   ├── api/           # 로그인 API 호출
│   └── index.ts
├── add-to-cart/
├── toggle-theme/
└── like-post/
```

**사용 기준**:
- 여러 페이지에서 재사용되는가?
- 사용자 액션을 포함하는가?
- 비즈니스 가치를 제공하는가?

**주의사항**:
- 한 페이지에서만 사용되는 기능은 pages 내부에 배치
- features 간 직접 import 금지 (같은 레이어 cross-import 불가)

**출처**: https://feature-sliced.design/docs/reference/layers

---

### 5. Entities (엔티티 레이어)

**정의**: 실제 비즈니스 개념과 도메인 모델을 표현하는 레이어

**책임**:
- 비즈니스 엔티티의 데이터 모델 정의 (User, Product, Order 등)
- 엔티티별 API 함수
- 엔티티 검증 스키마
- 엔티티 UI 표현 (카드, 리스트 아이템 등)

**특징**:
- 엔티티 간 상호작용은 상위 레이어(Features/Pages)에서 처리
- `@x` 표기법으로 엔티티 간 명시적 연결 가능 (제한적 사용)

**예시**:
```
entities/
├── user/
│   ├── ui/            # UserCard, UserAvatar
│   ├── model/         # User 타입, 검증 스키마
│   ├── api/           # getUser, updateUser
│   └── index.ts
├── product/
├── order/
└── comment/
```

**사용 기준**:
- 데이터베이스 테이블/컬렉션과 매핑되는가?
- 비즈니스 도메인의 핵심 개념인가?
- 여러 기능에서 공통으로 사용되는가?

**주의사항**:
- entities 간 직접 import 원칙적으로 금지
- 필요시 `@x` 표기법 사용하되 남용 금지

**출처**: https://feature-sliced.design/docs/reference/layers

---

### 6. Shared (공유 레이어)

**정의**: 프로젝트 독립적인 재사용 가능 기능을 제공하는 기반 레이어

**책임**:
- UI 키트 (비즈니스 로직 없는 순수 컴포넌트)
- 유틸리티 함수 (날짜, 텍스트, 색상 처리 등)
- 백엔드 API 클라이언트 설정
- 환경 변수 및 설정
- 공통 타입 정의

**특징**:
- 슬라이스를 포함하지 않고 세그먼트만 포함
- 세그먼트 간 자유로운 참조 가능
- 다른 레이어를 import 불가 (순수한 기반 계층)

**예시**:
```
shared/
├── ui/                # Button, Input, Modal 등 UI 컴포넌트
├── lib/               # formatDate, capitalize 등 유틸리티
├── api/               # axios/fetch 클라이언트 설정
├── config/            # 환경 변수, 기능 플래그
├── types/             # 공통 타입 정의
└── constants/         # 상수
```

**사용 기준**:
- 프로젝트 외부로 분리해도 작동하는가?
- 비즈니스 로직이 없는가?
- 다른 프로젝트에서도 재사용 가능한가?

**주의사항**:
- 비즈니스 로직 포함 금지
- shared는 다른 레이어를 import하지 않음

**출처**: https://feature-sliced.design/docs/reference/layers

---

## 슬라이스(Slices) - 비즈니스 도메인 분할

### 정의
슬라이스는 비즈니스 도메인이나 제품 의미에 따라 코드를 그룹화하는 두 번째 계층입니다.

**출처**: https://feature-sliced.design/docs/reference/slices-segments

### 특징
- 비즈니스 도메인에 따라 이름 결정 (예: `auth`, `profile`, `checkout`)
- App과 Shared 레이어에는 슬라이스가 없음 (세그먼트만 존재)
- 독립적이고 높은 응집도 유지
- 각 슬라이스는 Public API(index.ts) 필수

### 핵심 규칙
> "슬라이스 내 모듈은 오직 아래 레이어의 슬라이스만 import 가능"
> "같은 레이어의 다른 슬라이스는 import 불가 (cross-import 금지)"

**예외**: `@x` 표기법으로 entities 레이어에서 제한적 cross-import 허용

**예시**:
```
features/
├── auth/              # 인증 슬라이스
├── profile-edit/      # 프로필 편집 슬라이스
└── post-create/       # 게시물 생성 슬라이스
```

**출처**: https://feature-sliced.design/docs/reference/slices-segments

---

## 세그먼트(Segments) - 기술적 목적 분류

### 정의
세그먼트는 기술적 성질에 따라 코드를 그룹화하는 세 번째 계층입니다.

**출처**: https://feature-sliced.design/docs/reference/slices-segments

### 표준 세그먼트

#### `ui/`
- UI 컴포넌트
- 스타일 파일
- 시각적 표현

#### `api/`
- 백엔드 API 호출 함수
- 요청/응답 타입
- API 클라이언트 설정

#### `model/`
- 데이터 모델
- 비즈니스 로직
- 상태 관리 (stores, actions, reducers)
- 검증 스키마

#### `lib/`
- 해당 슬라이스 전용 유틸리티
- 헬퍼 함수
- 다른 모듈이 필요로 하는 라이브러리 코드

#### `config/`
- 설정 파일
- 기능 플래그
- 환경별 설정

#### `types/`
- 타입 정의
- 인터페이스
- (선택적: TypeScript 프로젝트)

### 주의사항
- 세그먼트 이름은 목적을 나타내야 함
- "components", "hooks" 같은 기술 용어보다 "ui", "model" 사용
- 커스텀 세그먼트 생성 가능하나 표준 세그먼트 우선 사용

**예시**:
```
features/auth/
├── ui/
│   ├── LoginForm.tsx
│   └── SignupForm.tsx
├── model/
│   ├── authStore.ts
│   └── validation.ts
├── api/
│   └── authApi.ts
└── index.ts         # Public API
```

**출처**: https://feature-sliced.design/docs/reference/slices-segments

---

## Public API - 모듈 간 계약

### 정의
Public API는 슬라이스와 같은 모듈 그룹 간의 계약으로, `index.ts` 파일을 통한 재내보내기로 구현됩니다.

**출처**: https://feature-sliced.design/docs/reference/public-api

### 목적
1. **구조 변경 보호** - 리팩토링 같은 내부 변화로부터 애플리케이션 보호
2. **동작 변경 반영** - 중요한 동작 변화는 API 변경으로 표현
3. **최소 노출** - 필요한 부분만 외부에 공개

### 규칙
- 모든 슬라이스는 `index.ts` 파일로 Public API 정의 필수
- 다른 슬라이스는 Public API를 통해서만 접근
- 내부 구조 직접 접근 금지

### 좋은 예시
```typescript
// features/auth/index.ts
export { LoginForm, SignupForm } from './ui';
export { useAuth } from './model';
export type { AuthUser } from './model';
```

### 나쁜 예시
```typescript
// 🚨 잘못된 사용: Public API 무시하고 직접 접근
import { LoginForm } from '@/features/auth/ui/LoginForm';

// ✅ 올바른 사용: Public API 사용
import { LoginForm } from '@/features/auth';
```

### 순환 참조 방지
- 같은 슬라이스 내에서는 상대 경로 사용
- 다른 슬라이스에서는 절대 경로와 Public API 사용

**출처**: https://feature-sliced.design/docs/reference/public-api

---

## 레이어 간 의존성 규칙

### 핵심 규칙

**"모듈은 같은 레이어나 위의 레이어에 있는 모듈을 참조할 수 없습니다"**

**출처**: https://feature-sliced.design/docs/get-started/overview

### 의존성 방향 (위에서 아래로만)

```
app/        → pages, widgets, features, entities, shared 모두 import 가능
pages/      → widgets, features, entities, shared import 가능
widgets/    → features, entities, shared import 가능
features/   → entities, shared import 가능
entities/   → shared만 import 가능
shared/     → 아무것도 import 불가 (순수 기반 계층)
```

### 같은 레이어 간 규칙

**원칙**: 같은 레이어의 슬라이스끼리는 서로 import 불가 (cross-import 금지)

**이유**:
- 높은 응집도 유지
- 순환 참조 방지
- 독립적인 모듈 단위 유지
- 변경 영향 범위 최소화

**예외**: entities 레이어에서만 `@x` 표기법으로 제한적 cross-import 허용

**예시**:
```typescript
// 🚨 잘못된 사용: 같은 레이어 cross-import
// features/auth/model/authStore.ts
import { userProfile } from '@/features/profile';  // ❌

// ✅ 올바른 사용: 하위 레이어 사용
// features/auth/model/authStore.ts
import { User } from '@/entities/user';  // ✅
```

**출처**: https://feature-sliced.design/docs/reference/layers

### 규칙이 필요한 이유

1. **예측 가능성**
   - 의존성 방향이 명확하여 코드 이해 용이
   - 변경 영향 범위 예측 가능

2. **안정성**
   - 하위 레이어 변경이 상위 레이어에 영향 최소화
   - 순환 참조 원천 차단

3. **테스트 용이성**
   - 하위 레이어부터 독립적으로 테스트 가능
   - Mock 생성 간소화

4. **확장성**
   - 새 기능 추가 시 기존 코드 영향 최소화
   - 레이어별 독립적 개발 가능

**출처**: https://feature-sliced.design/docs/get-started/overview

---

## 흔한 안티패턴

### 1. Desegmented (분산된 구조)

**문제**: 특정 도메인 관련 모듈들이 불필요하게 산재

**예시**:
```
components/DeliveryCard
actions/delivery.js
constants/delivery.js
helpers/delivery.js
entities/delivery/
```

**문제점**:
- 높은 응집도 원칙 위반
- 특정 기능 이해를 위해 프로젝트 전체 탐색 필요
- 변경 범위 확대

**해결책**:
```
entities/delivery/
├── ui/           # DeliveryCard
├── model/        # actions, constants
└── lib/          # helpers
```

**출처**: https://feature-sliced.design/docs/guides/issues/desegmented

### 2. God Slice (거대 슬라이스)

**문제**: 너무 많은 책임을 가진 거대한 슬라이스

**해결책**: 비즈니스 도메인에 따라 더 작은 슬라이스로 분리

### 3. Cross-Import 위반

**문제**: 같은 레이어의 슬라이스끼리 직접 import

**해결책**:
- 공통 로직은 하위 레이어(entities, shared)로 이동
- 조합 로직은 상위 레이어(pages, widgets)로 이동

### 4. Public API 무시

**문제**: `index.ts`를 무시하고 내부 구조 직접 접근

**해결책**: 항상 Public API를 통해 접근

**출처**: https://feature-sliced.design/docs/reference/public-api

---

## 점검 항목

### 1. 폴더 구조 검증
- [ ] src/ 하위에 FSD 레이어 폴더들이 올바르게 존재하는지 확인
  - `app/` - 애플리케이션 전역 설정
  - `pages/` - 페이지/화면 구성
  - `widgets/` - 독립적인 대규모 UI 블록 (선택적)
  - `features/` - 재사용 가능한 사용자 기능
  - `entities/` - 비즈니스 엔티티
  - `shared/` - 프로젝트 독립적 재사용 코드
- [ ] FSD 레이어가 아닌 폴더가 src/ 직하위에 있는지 확인
- [ ] `processes/` 레이어가 있다면 deprecated 경고

### 2. 슬라이스(Slice) 구조 검증
- [ ] 각 레이어(pages, widgets, features, entities) 내부 슬라이스들이 적절한 세그먼트 구조를 가지는지
  - `ui/` - UI 컴포넌트
  - `model/` - 비즈니스 로직, 상태
  - `api/` - 백엔드 호출
  - `lib/` - 유틸리티
  - `config/` - 설정 (필요 시)
- [ ] 모든 슬라이스가 `index.ts` (Public API) 제공하는지
- [ ] App과 Shared 레이어는 슬라이스 없이 세그먼트만 포함하는지

### 3. Import 의존성 규칙 검증
- [ ] **하위 레이어만 import** - 상위/같은 레이어 import 금지
  - 레이어 순서: `shared` < `entities` < `features` < `widgets` < `pages` < `app`
  - 예: `features`는 `entities`, `shared`만 import 가능
  - 예: `pages`는 `widgets`, `features`, `entities`, `shared` import 가능
- [ ] **같은 레이어 cross-import 금지**
  - 예: `features/auth`가 `features/user` import하면 ❌
  - 예외: entities에서 `@x` 표기법만 허용
- [ ] **Shared는 완전히 독립적** - 다른 레이어 import 불가
- [ ] **Public API 사용** - `index.ts`를 통해서만 접근

### 4. 파일 배치 적절성 검증

#### Features 레이어
- [ ] 재사용 가능한 사용자 액션/기능이 features에 위치
- [ ] 한 페이지에서만 사용되는 기능은 pages 내부에 위치
- [ ] 여러 entities 조합 로직이 features에 위치

#### Entities 레이어
- [ ] 비즈니스 엔티티(User, Product 등)가 entities에 위치
- [ ] 엔티티의 데이터 모델, API 함수가 entities에 위치
- [ ] 엔티티 간 상호작용 로직은 상위 레이어에 위치

#### Widgets 레이어
- [ ] 여러 페이지에서 재사용되는 대규모 UI가 widgets에 위치
- [ ] 독립적인 UI 청크가 widgets에 위치
- [ ] 페이지 특화 UI는 pages 내부에 위치

#### Shared 레이어
- [ ] 비즈니스 로직 없는 순수 UI 컴포넌트가 shared/ui에 위치
- [ ] 프로젝트 독립적 유틸리티가 shared/lib에 위치
- [ ] API 클라이언트 설정이 shared/api에 위치
- [ ] 공통 타입이 shared/types에 위치

#### App 레이어
- [ ] 라우팅 설정이 app/routes에 위치
- [ ] 전역 상태가 app/store에 위치
- [ ] 전역 스타일이 app/styles에 위치
- [ ] 앱 진입점이 app에 위치

### 5. 안티패턴 검증
- [ ] **Desegmented 패턴 확인** - 같은 도메인 코드가 여러 곳에 산재되지 않는지
- [ ] **God Slice 확인** - 너무 거대한 슬라이스가 없는지
- [ ] **직접 접근 확인** - Public API 무시하고 내부 구조 직접 접근하지 않는지

## 작업 순서

1. `find src -type f \( -name "*.ts" -o -name "*.tsx" \)` 명령어로 모든 TypeScript 파일 목록 확인
2. src/ 직하위 폴더 구조를 확인하고, FSD 레이어가 아닌 폴더가 있는지 검증
3. 각 파일의 import 문을 분석하여 의존성 규칙 위반 사항 확인
4. 발견된 문제점을 카테고리별로 분류

## 출력 형식

검증 결과를 다음과 같이 카테고리별로 정리하여 출력:

### ✅ FSD 아키텍처 준수 현황

#### 📁 폴더 구조
- [정상/문제점] src/ 하위 레이어 구조 상태
- [문제가 있다면] FSD 레이어가 아닌 폴더 목록

#### 🔗 Import 의존성 규칙 위반
- [파일 경로]: 위반 내용 (예: features가 pages를 import)
- 위반 건수 총합

#### 🚨 같은 레이어 간 Cross-Import
- [파일 경로]: 같은 레이어의 어떤 slice를 import하는지
- 위반 건수 총합

#### 📦 파일 배치 이슈
- [파일 경로]: 어떤 레이어로 이동해야 하는지와 이유

#### 📊 종합 점수
- 전체 파일 수: X개
- 문제가 있는 파일 수: Y개
- 준수율: Z%

## 주의사항

- 별도의 파일을 생성하지 말고, 터미널에 결과를 바로 출력
- node_modules, dist, build 등 빌드 결과물은 검사 제외
- 문제가 없는 경우에도 "✅ FSD 아키텍처를 잘 준수하고 있습니다" 메시지 출력
- 각 위반 사항에 대해 구체적인 파일명과 라인 번호 제공
- 심각도 표시: 🚨 Critical (반드시 수정), ⚠️ Warning (권장 수정)
