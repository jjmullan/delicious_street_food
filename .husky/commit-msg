#!/usr/bin/env sh

# PATH 설정 (GUI 환경에서도 pnpm을 찾을 수 있도록)
export PATH="/usr/local/bin:$HOME/.local/share/pnpm:$PATH"

# pnpm이 설치되어 있는지 확인
if ! command -v pnpm >/dev/null 2>&1; then
  echo "Error: pnpm not found. Please install pnpm first."
  exit 1
fi

pnpm exec commitlint --edit $1
