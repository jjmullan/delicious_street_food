// 지구의 평균 반지름 (미터 단위)
export const EARTH_RADIUS_METERS = 6_371_000;

// 위치 생성 시 기존 위치로부터의 최소 거리 (미터 단위)
export const MINIMUM_LOCATION_DISTANCE_METERS = 10;

// 위치 생성 시 현재 위치로부터의 최대 거리 (미터 단위) - 1km
export const MAXIMUM_DISTANCE_FROM_CURRENT_LOCATION_METERS = 3_000;

// 도(degree)를 라디안(radian)으로 변환하는 상수
export const DEGREES_TO_RADIANS = Math.PI / 180;
